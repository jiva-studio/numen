// Package search holds the one search this application does.
//
// Lexical and dense retrieval are not two searches. One search takes the query
// and says which halves to run and how many candidates each keeps, and returns
// one kind of result.
package search
