// Package embedding holds the rules a vector obeys, independent of who
// produced it: what a stored vector looks like, and how many texts one request
// to a model may carry.
//
// Both are agreements between whoever produces a vector and whoever reads it,
// so they are stated once, here, and every embedder obeys them.
package embedding
