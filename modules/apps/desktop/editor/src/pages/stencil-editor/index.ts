/** The stencil tab: the fields and faces a deck's cards are cut to. */
export { default as StencilTab } from './ui/StencilTab.vue'
export { useStencilTabs } from './kind'
export type { StencilTabState } from './types'
