/**
 * What a run is, as the media entity asks about one.
 *
 * A recording carries a transcript and a page carries the prose it is written
 * around, so a medium is what it is partly by what has been made from it. This
 * is the whole of what one entity says to the other.
 */
export { running } from '../api/artifacts'
export type { ArtifactStates } from '../types'
