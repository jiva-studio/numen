/** The vaults an installation holds: what one is, what changes the list, and what it reports. */
export type { Vault, VaultErrorCode, VaultList, VaultResult, Vaults } from './types'
export { VAULT_ERRORS } from './words'
