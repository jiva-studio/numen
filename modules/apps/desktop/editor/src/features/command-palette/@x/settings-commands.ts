/**
 * What a list is, as the settings commands build one.
 *
 * The settings hold the lists and the palette draws them, so the two features
 * meet at the shape of a row. This is the whole of what one says to the other.
 */
export type { StepGroup, StepRow } from '../types'
