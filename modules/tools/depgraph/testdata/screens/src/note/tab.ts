/** A screen reaching a shared folder and the window's root: both permitted. */
import { puts } from '../tabs/putting'
import { WORDS } from '../words'

export const held = () => `${puts()} ${WORDS.going}`
