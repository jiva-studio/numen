/**
 * A folder nobody named as shared. It is a screen because it is not one of the
 * five, which is how a folder added tomorrow is covered without being listed.
 */
import { held } from '../note/tab'

export const entries = () => held()
