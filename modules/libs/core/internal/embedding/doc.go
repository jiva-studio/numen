// Package embedding is what a vector is and what makes one: the shape a stored
// vector takes, how many texts one request carries, whether two models are one,
// and a model that is still arriving.
package embedding
