// Package embedders opens what makes the vectors a vault is searched by and
// what makes the vector a question is asked with.
//
// The two are one model or the comparison between them means nothing, and
// nothing in a settings file shows that two providers are one: a repository and
// a service call one model by two names, and two services answer to one name
// with two sets of weights. So the two are asked one text and held to answering
// it alike. A provider that disagrees is let go of and a question is answered by
// the words, which is a whole search.
//
// A model on this machine is fetched and compiled behind the window, and how far
// that has got is one line in the list of what is being done. Until it is here a
// pass that fills an index waits and a person who has typed a question does not.
//
// Which provider answers, and what its settings are, is settled before anything
// here is called: a provider is opened one way or the other, and this package
// reads nothing about where it stands.
package embedders
