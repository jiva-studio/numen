// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: numen/v1/workspace.proto

// What a client may ask about where the person stands in the vault a window is
// showing.
//
// The tabs the window has open and the places something else asks to be put in
// front of the person are the window's. What the vault is and what has changed
// in it is vault.proto.
package numenv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jiva-studio/numen/modules/libs/protocol/gen/numen/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorkspaceServiceName is the fully-qualified name of the WorkspaceService service.
	WorkspaceServiceName = "numen.v1.WorkspaceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorkspaceServiceWatchFocusProcedure is the fully-qualified name of the WorkspaceService's
	// WatchFocus RPC.
	WorkspaceServiceWatchFocusProcedure = "/numen.v1.WorkspaceService/WatchFocus"
	// WorkspaceServiceWriteOpenTabsProcedure is the fully-qualified name of the WorkspaceService's
	// WriteOpenTabs RPC.
	WorkspaceServiceWriteOpenTabsProcedure = "/numen.v1.WorkspaceService/WriteOpenTabs"
)

// WorkspaceServiceClient is a client for the numen.v1.WorkspaceService service.
type WorkspaceServiceClient interface {
	// WatchFocus reports the places something else asked to be put in front of
	// the person — an agent working the vault beside them — for as long as the
	// caller listens. What travelling there looks like is the client's.
	WatchFocus(context.Context, *connect.Request[v1.WatchFocusRequest]) (*connect.ServerStreamForClient[v1.WatchFocusResponse], error)
	// WriteOpenTabs says what the person has open — every tab of the window, and
	// which of them is in front. The client says so again whenever any of it
	// changes, and an agent working the vault beside them reads what it last
	// said.
	WriteOpenTabs(context.Context, *connect.Request[v1.WriteOpenTabsRequest]) (*connect.Response[v1.WriteOpenTabsResponse], error)
}

// NewWorkspaceServiceClient constructs a client for the numen.v1.WorkspaceService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorkspaceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorkspaceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	workspaceServiceMethods := v1.File_numen_v1_workspace_proto.Services().ByName("WorkspaceService").Methods()
	return &workspaceServiceClient{
		watchFocus: connect.NewClient[v1.WatchFocusRequest, v1.WatchFocusResponse](
			httpClient,
			baseURL+WorkspaceServiceWatchFocusProcedure,
			connect.WithSchema(workspaceServiceMethods.ByName("WatchFocus")),
			connect.WithClientOptions(opts...),
		),
		writeOpenTabs: connect.NewClient[v1.WriteOpenTabsRequest, v1.WriteOpenTabsResponse](
			httpClient,
			baseURL+WorkspaceServiceWriteOpenTabsProcedure,
			connect.WithSchema(workspaceServiceMethods.ByName("WriteOpenTabs")),
			connect.WithClientOptions(opts...),
		),
	}
}

// workspaceServiceClient implements WorkspaceServiceClient.
type workspaceServiceClient struct {
	watchFocus    *connect.Client[v1.WatchFocusRequest, v1.WatchFocusResponse]
	writeOpenTabs *connect.Client[v1.WriteOpenTabsRequest, v1.WriteOpenTabsResponse]
}

// WatchFocus calls numen.v1.WorkspaceService.WatchFocus.
func (c *workspaceServiceClient) WatchFocus(ctx context.Context, req *connect.Request[v1.WatchFocusRequest]) (*connect.ServerStreamForClient[v1.WatchFocusResponse], error) {
	return c.watchFocus.CallServerStream(ctx, req)
}

// WriteOpenTabs calls numen.v1.WorkspaceService.WriteOpenTabs.
func (c *workspaceServiceClient) WriteOpenTabs(ctx context.Context, req *connect.Request[v1.WriteOpenTabsRequest]) (*connect.Response[v1.WriteOpenTabsResponse], error) {
	return c.writeOpenTabs.CallUnary(ctx, req)
}

// WorkspaceServiceHandler is an implementation of the numen.v1.WorkspaceService service.
type WorkspaceServiceHandler interface {
	// WatchFocus reports the places something else asked to be put in front of
	// the person — an agent working the vault beside them — for as long as the
	// caller listens. What travelling there looks like is the client's.
	WatchFocus(context.Context, *connect.Request[v1.WatchFocusRequest], *connect.ServerStream[v1.WatchFocusResponse]) error
	// WriteOpenTabs says what the person has open — every tab of the window, and
	// which of them is in front. The client says so again whenever any of it
	// changes, and an agent working the vault beside them reads what it last
	// said.
	WriteOpenTabs(context.Context, *connect.Request[v1.WriteOpenTabsRequest]) (*connect.Response[v1.WriteOpenTabsResponse], error)
}

// NewWorkspaceServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorkspaceServiceHandler(svc WorkspaceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	workspaceServiceMethods := v1.File_numen_v1_workspace_proto.Services().ByName("WorkspaceService").Methods()
	workspaceServiceWatchFocusHandler := connect.NewServerStreamHandler(
		WorkspaceServiceWatchFocusProcedure,
		svc.WatchFocus,
		connect.WithSchema(workspaceServiceMethods.ByName("WatchFocus")),
		connect.WithHandlerOptions(opts...),
	)
	workspaceServiceWriteOpenTabsHandler := connect.NewUnaryHandler(
		WorkspaceServiceWriteOpenTabsProcedure,
		svc.WriteOpenTabs,
		connect.WithSchema(workspaceServiceMethods.ByName("WriteOpenTabs")),
		connect.WithHandlerOptions(opts...),
	)
	return "/numen.v1.WorkspaceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorkspaceServiceWatchFocusProcedure:
			workspaceServiceWatchFocusHandler.ServeHTTP(w, r)
		case WorkspaceServiceWriteOpenTabsProcedure:
			workspaceServiceWriteOpenTabsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorkspaceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedWorkspaceServiceHandler struct{}

func (UnimplementedWorkspaceServiceHandler) WatchFocus(context.Context, *connect.Request[v1.WatchFocusRequest], *connect.ServerStream[v1.WatchFocusResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.WorkspaceService.WatchFocus is not implemented"))
}

func (UnimplementedWorkspaceServiceHandler) WriteOpenTabs(context.Context, *connect.Request[v1.WriteOpenTabsRequest]) (*connect.Response[v1.WriteOpenTabsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.WorkspaceService.WriteOpenTabs is not implemented"))
}
