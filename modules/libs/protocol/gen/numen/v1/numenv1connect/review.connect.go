// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: numen/v1/review.proto

// What a client running a person's cards may ask.
//
// Review is an application of its own, beside the editor and over the same core
// (ADR-0030), so this service stands beside the editor's and shares nothing with
// it. It reaches every vault the installation knows, because what a person owes
// today is owed across all of them.
//
// A card is laid out here, unlike the cards service, which hands a face over as
// the markdown it was written as: what a person is shown is the face filled with
// one card's values, and nothing above this has the two halves to fill it with.
package numenv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jiva-studio/numen/modules/libs/protocol/gen/numen/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ReviewServiceName is the fully-qualified name of the ReviewService service.
	ReviewServiceName = "numen.v1.ReviewService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ReviewServiceOwingProcedure is the fully-qualified name of the ReviewService's Owing RPC.
	ReviewServiceOwingProcedure = "/numen.v1.ReviewService/Owing"
	// ReviewServiceStartProcedure is the fully-qualified name of the ReviewService's Start RPC.
	ReviewServiceStartProcedure = "/numen.v1.ReviewService/Start"
	// ReviewServiceAnswerProcedure is the fully-qualified name of the ReviewService's Answer RPC.
	ReviewServiceAnswerProcedure = "/numen.v1.ReviewService/Answer"
	// ReviewServiceTakeBackProcedure is the fully-qualified name of the ReviewService's TakeBack RPC.
	ReviewServiceTakeBackProcedure = "/numen.v1.ReviewService/TakeBack"
)

// ReviewServiceClient is a client for the numen.v1.ReviewService service.
type ReviewServiceClient interface {
	// Owing is what every vault the installation knows comes to today: how much
	// it holds, how much is due, and how much has never been asked. It is what
	// the application opens on.
	//
	// A vault the index does not carry yet is listed with nothing counted and
	// says so: the list of its decks is the index's answer, and this application
	// builds none.
	Owing(context.Context, *connect.Request[v1.OwingRequest]) (*connect.Response[v1.OwingResponse], error)
	// Start opens a run and hands over what to ask, in order. A run writes one
	// file of its own in the vault and nothing else ever appends to it.
	//
	// Naming a deck asks that deck alone. Naming none asks the whole vault, which
	// is the ordinary way to sit down to this.
	Start(context.Context, *connect.Request[v1.StartRequest]) (*connect.Response[v1.StartResponse], error)
	// Answer writes down how a card came back. What comes back is the identifier
	// of the line, which is what taking that answer back names.
	Answer(context.Context, *connect.Request[v1.AnswerRequest]) (*connect.Response[v1.AnswerResponse], error)
	// TakeBack writes down that an answer was taken back. Both lines stay in the
	// file: nothing in a log is ever rewritten or removed.
	TakeBack(context.Context, *connect.Request[v1.TakeBackRequest]) (*connect.Response[v1.TakeBackResponse], error)
}

// NewReviewServiceClient constructs a client for the numen.v1.ReviewService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewReviewServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ReviewServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	reviewServiceMethods := v1.File_numen_v1_review_proto.Services().ByName("ReviewService").Methods()
	return &reviewServiceClient{
		owing: connect.NewClient[v1.OwingRequest, v1.OwingResponse](
			httpClient,
			baseURL+ReviewServiceOwingProcedure,
			connect.WithSchema(reviewServiceMethods.ByName("Owing")),
			connect.WithClientOptions(opts...),
		),
		start: connect.NewClient[v1.StartRequest, v1.StartResponse](
			httpClient,
			baseURL+ReviewServiceStartProcedure,
			connect.WithSchema(reviewServiceMethods.ByName("Start")),
			connect.WithClientOptions(opts...),
		),
		answer: connect.NewClient[v1.AnswerRequest, v1.AnswerResponse](
			httpClient,
			baseURL+ReviewServiceAnswerProcedure,
			connect.WithSchema(reviewServiceMethods.ByName("Answer")),
			connect.WithClientOptions(opts...),
		),
		takeBack: connect.NewClient[v1.TakeBackRequest, v1.TakeBackResponse](
			httpClient,
			baseURL+ReviewServiceTakeBackProcedure,
			connect.WithSchema(reviewServiceMethods.ByName("TakeBack")),
			connect.WithClientOptions(opts...),
		),
	}
}

// reviewServiceClient implements ReviewServiceClient.
type reviewServiceClient struct {
	owing    *connect.Client[v1.OwingRequest, v1.OwingResponse]
	start    *connect.Client[v1.StartRequest, v1.StartResponse]
	answer   *connect.Client[v1.AnswerRequest, v1.AnswerResponse]
	takeBack *connect.Client[v1.TakeBackRequest, v1.TakeBackResponse]
}

// Owing calls numen.v1.ReviewService.Owing.
func (c *reviewServiceClient) Owing(ctx context.Context, req *connect.Request[v1.OwingRequest]) (*connect.Response[v1.OwingResponse], error) {
	return c.owing.CallUnary(ctx, req)
}

// Start calls numen.v1.ReviewService.Start.
func (c *reviewServiceClient) Start(ctx context.Context, req *connect.Request[v1.StartRequest]) (*connect.Response[v1.StartResponse], error) {
	return c.start.CallUnary(ctx, req)
}

// Answer calls numen.v1.ReviewService.Answer.
func (c *reviewServiceClient) Answer(ctx context.Context, req *connect.Request[v1.AnswerRequest]) (*connect.Response[v1.AnswerResponse], error) {
	return c.answer.CallUnary(ctx, req)
}

// TakeBack calls numen.v1.ReviewService.TakeBack.
func (c *reviewServiceClient) TakeBack(ctx context.Context, req *connect.Request[v1.TakeBackRequest]) (*connect.Response[v1.TakeBackResponse], error) {
	return c.takeBack.CallUnary(ctx, req)
}

// ReviewServiceHandler is an implementation of the numen.v1.ReviewService service.
type ReviewServiceHandler interface {
	// Owing is what every vault the installation knows comes to today: how much
	// it holds, how much is due, and how much has never been asked. It is what
	// the application opens on.
	//
	// A vault the index does not carry yet is listed with nothing counted and
	// says so: the list of its decks is the index's answer, and this application
	// builds none.
	Owing(context.Context, *connect.Request[v1.OwingRequest]) (*connect.Response[v1.OwingResponse], error)
	// Start opens a run and hands over what to ask, in order. A run writes one
	// file of its own in the vault and nothing else ever appends to it.
	//
	// Naming a deck asks that deck alone. Naming none asks the whole vault, which
	// is the ordinary way to sit down to this.
	Start(context.Context, *connect.Request[v1.StartRequest]) (*connect.Response[v1.StartResponse], error)
	// Answer writes down how a card came back. What comes back is the identifier
	// of the line, which is what taking that answer back names.
	Answer(context.Context, *connect.Request[v1.AnswerRequest]) (*connect.Response[v1.AnswerResponse], error)
	// TakeBack writes down that an answer was taken back. Both lines stay in the
	// file: nothing in a log is ever rewritten or removed.
	TakeBack(context.Context, *connect.Request[v1.TakeBackRequest]) (*connect.Response[v1.TakeBackResponse], error)
}

// NewReviewServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewReviewServiceHandler(svc ReviewServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	reviewServiceMethods := v1.File_numen_v1_review_proto.Services().ByName("ReviewService").Methods()
	reviewServiceOwingHandler := connect.NewUnaryHandler(
		ReviewServiceOwingProcedure,
		svc.Owing,
		connect.WithSchema(reviewServiceMethods.ByName("Owing")),
		connect.WithHandlerOptions(opts...),
	)
	reviewServiceStartHandler := connect.NewUnaryHandler(
		ReviewServiceStartProcedure,
		svc.Start,
		connect.WithSchema(reviewServiceMethods.ByName("Start")),
		connect.WithHandlerOptions(opts...),
	)
	reviewServiceAnswerHandler := connect.NewUnaryHandler(
		ReviewServiceAnswerProcedure,
		svc.Answer,
		connect.WithSchema(reviewServiceMethods.ByName("Answer")),
		connect.WithHandlerOptions(opts...),
	)
	reviewServiceTakeBackHandler := connect.NewUnaryHandler(
		ReviewServiceTakeBackProcedure,
		svc.TakeBack,
		connect.WithSchema(reviewServiceMethods.ByName("TakeBack")),
		connect.WithHandlerOptions(opts...),
	)
	return "/numen.v1.ReviewService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ReviewServiceOwingProcedure:
			reviewServiceOwingHandler.ServeHTTP(w, r)
		case ReviewServiceStartProcedure:
			reviewServiceStartHandler.ServeHTTP(w, r)
		case ReviewServiceAnswerProcedure:
			reviewServiceAnswerHandler.ServeHTTP(w, r)
		case ReviewServiceTakeBackProcedure:
			reviewServiceTakeBackHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedReviewServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedReviewServiceHandler struct{}

func (UnimplementedReviewServiceHandler) Owing(context.Context, *connect.Request[v1.OwingRequest]) (*connect.Response[v1.OwingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.ReviewService.Owing is not implemented"))
}

func (UnimplementedReviewServiceHandler) Start(context.Context, *connect.Request[v1.StartRequest]) (*connect.Response[v1.StartResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.ReviewService.Start is not implemented"))
}

func (UnimplementedReviewServiceHandler) Answer(context.Context, *connect.Request[v1.AnswerRequest]) (*connect.Response[v1.AnswerResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.ReviewService.Answer is not implemented"))
}

func (UnimplementedReviewServiceHandler) TakeBack(context.Context, *connect.Request[v1.TakeBackRequest]) (*connect.Response[v1.TakeBackResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.ReviewService.TakeBack is not implemented"))
}
