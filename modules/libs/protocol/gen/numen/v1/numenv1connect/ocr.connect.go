// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: numen/v1/ocr.proto

// The text a model read off the pages of a document, and where on those pages
// each run of it stands.
//
// The text is what the index counts in and what a person reads; the boxes are
// an overlay on it, and a reader draws them over the page it is showing.
package numenv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jiva-studio/numen/modules/libs/protocol/gen/numen/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// OcrServiceName is the fully-qualified name of the OcrService service.
	OcrServiceName = "numen.v1.OcrService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// OcrServiceReadOcrProcedure is the fully-qualified name of the OcrService's ReadOcr RPC.
	OcrServiceReadOcrProcedure = "/numen.v1.OcrService/ReadOcr"
)

// OcrServiceClient is a client for the numen.v1.OcrService service.
type OcrServiceClient interface {
	// ReadOcr is the runs of the text asked about, each as it reads and with the
	// boxes covering it. The answers stand in the order the runs were asked
	// about, so a caller that asked about a passage and the places around it
	// knows which is which.
	ReadOcr(context.Context, *connect.Request[v1.ReadOcrRequest]) (*connect.Response[v1.ReadOcrResponse], error)
}

// NewOcrServiceClient constructs a client for the numen.v1.OcrService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewOcrServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) OcrServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	ocrServiceMethods := v1.File_numen_v1_ocr_proto.Services().ByName("OcrService").Methods()
	return &ocrServiceClient{
		readOcr: connect.NewClient[v1.ReadOcrRequest, v1.ReadOcrResponse](
			httpClient,
			baseURL+OcrServiceReadOcrProcedure,
			connect.WithSchema(ocrServiceMethods.ByName("ReadOcr")),
			connect.WithClientOptions(opts...),
		),
	}
}

// ocrServiceClient implements OcrServiceClient.
type ocrServiceClient struct {
	readOcr *connect.Client[v1.ReadOcrRequest, v1.ReadOcrResponse]
}

// ReadOcr calls numen.v1.OcrService.ReadOcr.
func (c *ocrServiceClient) ReadOcr(ctx context.Context, req *connect.Request[v1.ReadOcrRequest]) (*connect.Response[v1.ReadOcrResponse], error) {
	return c.readOcr.CallUnary(ctx, req)
}

// OcrServiceHandler is an implementation of the numen.v1.OcrService service.
type OcrServiceHandler interface {
	// ReadOcr is the runs of the text asked about, each as it reads and with the
	// boxes covering it. The answers stand in the order the runs were asked
	// about, so a caller that asked about a passage and the places around it
	// knows which is which.
	ReadOcr(context.Context, *connect.Request[v1.ReadOcrRequest]) (*connect.Response[v1.ReadOcrResponse], error)
}

// NewOcrServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewOcrServiceHandler(svc OcrServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	ocrServiceMethods := v1.File_numen_v1_ocr_proto.Services().ByName("OcrService").Methods()
	ocrServiceReadOcrHandler := connect.NewUnaryHandler(
		OcrServiceReadOcrProcedure,
		svc.ReadOcr,
		connect.WithSchema(ocrServiceMethods.ByName("ReadOcr")),
		connect.WithHandlerOptions(opts...),
	)
	return "/numen.v1.OcrService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case OcrServiceReadOcrProcedure:
			ocrServiceReadOcrHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedOcrServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedOcrServiceHandler struct{}

func (UnimplementedOcrServiceHandler) ReadOcr(context.Context, *connect.Request[v1.ReadOcrRequest]) (*connect.Response[v1.ReadOcrResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.OcrService.ReadOcr is not implemented"))
}
