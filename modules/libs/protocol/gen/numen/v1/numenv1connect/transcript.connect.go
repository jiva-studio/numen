// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: numen/v1/transcript.proto

// The text of a file with the times each stretch of it was said at.
//
// Two things write one: a model listening to a recording, and a site publishing
// words with a video. A transcript is a transcript either way, so one service
// answers for both.
package numenv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jiva-studio/numen/modules/libs/protocol/gen/numen/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TranscriptServiceName is the fully-qualified name of the TranscriptService service.
	TranscriptServiceName = "numen.v1.TranscriptService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TranscriptServiceReadTranscriptProcedure is the fully-qualified name of the TranscriptService's
	// ReadTranscript RPC.
	TranscriptServiceReadTranscriptProcedure = "/numen.v1.TranscriptService/ReadTranscript"
	// TranscriptServiceWriteTranscriptProcedure is the fully-qualified name of the TranscriptService's
	// WriteTranscript RPC.
	TranscriptServiceWriteTranscriptProcedure = "/numen.v1.TranscriptService/WriteTranscript"
)

// TranscriptServiceClient is a client for the numen.v1.TranscriptService service.
type TranscriptServiceClient interface {
	// ReadTranscript is the words as they now stand: the ones a person put right,
	// and what a model heard where nothing put them right.
	ReadTranscript(context.Context, *connect.Request[v1.ReadTranscriptRequest]) (*connect.Response[v1.ReadTranscriptResponse], error)
	// WriteTranscript writes the words as a person left them.
	//
	// What the model heard stays under its own name and the words as they now
	// stand go beside it, so a transcript edited into nonsense is corrections
	// that can be taken away and what was heard comes back.
	WriteTranscript(context.Context, *connect.Request[v1.WriteTranscriptRequest]) (*connect.Response[v1.WriteTranscriptResponse], error)
}

// NewTranscriptServiceClient constructs a client for the numen.v1.TranscriptService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTranscriptServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TranscriptServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	transcriptServiceMethods := v1.File_numen_v1_transcript_proto.Services().ByName("TranscriptService").Methods()
	return &transcriptServiceClient{
		readTranscript: connect.NewClient[v1.ReadTranscriptRequest, v1.ReadTranscriptResponse](
			httpClient,
			baseURL+TranscriptServiceReadTranscriptProcedure,
			connect.WithSchema(transcriptServiceMethods.ByName("ReadTranscript")),
			connect.WithClientOptions(opts...),
		),
		writeTranscript: connect.NewClient[v1.WriteTranscriptRequest, v1.WriteTranscriptResponse](
			httpClient,
			baseURL+TranscriptServiceWriteTranscriptProcedure,
			connect.WithSchema(transcriptServiceMethods.ByName("WriteTranscript")),
			connect.WithClientOptions(opts...),
		),
	}
}

// transcriptServiceClient implements TranscriptServiceClient.
type transcriptServiceClient struct {
	readTranscript  *connect.Client[v1.ReadTranscriptRequest, v1.ReadTranscriptResponse]
	writeTranscript *connect.Client[v1.WriteTranscriptRequest, v1.WriteTranscriptResponse]
}

// ReadTranscript calls numen.v1.TranscriptService.ReadTranscript.
func (c *transcriptServiceClient) ReadTranscript(ctx context.Context, req *connect.Request[v1.ReadTranscriptRequest]) (*connect.Response[v1.ReadTranscriptResponse], error) {
	return c.readTranscript.CallUnary(ctx, req)
}

// WriteTranscript calls numen.v1.TranscriptService.WriteTranscript.
func (c *transcriptServiceClient) WriteTranscript(ctx context.Context, req *connect.Request[v1.WriteTranscriptRequest]) (*connect.Response[v1.WriteTranscriptResponse], error) {
	return c.writeTranscript.CallUnary(ctx, req)
}

// TranscriptServiceHandler is an implementation of the numen.v1.TranscriptService service.
type TranscriptServiceHandler interface {
	// ReadTranscript is the words as they now stand: the ones a person put right,
	// and what a model heard where nothing put them right.
	ReadTranscript(context.Context, *connect.Request[v1.ReadTranscriptRequest]) (*connect.Response[v1.ReadTranscriptResponse], error)
	// WriteTranscript writes the words as a person left them.
	//
	// What the model heard stays under its own name and the words as they now
	// stand go beside it, so a transcript edited into nonsense is corrections
	// that can be taken away and what was heard comes back.
	WriteTranscript(context.Context, *connect.Request[v1.WriteTranscriptRequest]) (*connect.Response[v1.WriteTranscriptResponse], error)
}

// NewTranscriptServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTranscriptServiceHandler(svc TranscriptServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	transcriptServiceMethods := v1.File_numen_v1_transcript_proto.Services().ByName("TranscriptService").Methods()
	transcriptServiceReadTranscriptHandler := connect.NewUnaryHandler(
		TranscriptServiceReadTranscriptProcedure,
		svc.ReadTranscript,
		connect.WithSchema(transcriptServiceMethods.ByName("ReadTranscript")),
		connect.WithHandlerOptions(opts...),
	)
	transcriptServiceWriteTranscriptHandler := connect.NewUnaryHandler(
		TranscriptServiceWriteTranscriptProcedure,
		svc.WriteTranscript,
		connect.WithSchema(transcriptServiceMethods.ByName("WriteTranscript")),
		connect.WithHandlerOptions(opts...),
	)
	return "/numen.v1.TranscriptService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TranscriptServiceReadTranscriptProcedure:
			transcriptServiceReadTranscriptHandler.ServeHTTP(w, r)
		case TranscriptServiceWriteTranscriptProcedure:
			transcriptServiceWriteTranscriptHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTranscriptServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTranscriptServiceHandler struct{}

func (UnimplementedTranscriptServiceHandler) ReadTranscript(context.Context, *connect.Request[v1.ReadTranscriptRequest]) (*connect.Response[v1.ReadTranscriptResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.TranscriptService.ReadTranscript is not implemented"))
}

func (UnimplementedTranscriptServiceHandler) WriteTranscript(context.Context, *connect.Request[v1.WriteTranscriptRequest]) (*connect.Response[v1.WriteTranscriptResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numen.v1.TranscriptService.WriteTranscript is not implemented"))
}
