# A note inside a hidden folder

Hidden folders are skipped whole, whatever tool put them there. This one is
named .obsidian because that is what a real vault is likely to contain, but the
rule is about the leading dot, not about any particular application.
